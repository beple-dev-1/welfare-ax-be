# 프로파일 지정:
#   로컬 개발: -Dspring.profiles.active=local
#   개발 서버: -Dspring.profiles.active=dev
#   운영:      -Dspring.profiles.active=prod

spring:
  application:
    name: welfare-ax-user
  profiles:
    group:
      # local 프로파일 활성화 시 api 프로파일을 함께 활성화한다
      local:
        - api

server:
  port: 8080
