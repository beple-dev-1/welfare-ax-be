# 외부 API 연동 설정 파일
# 각 API의 기본 URL, 타임아웃, 인증 정보를 이 파일에서 관리한다.
# 운영 환경 민감값(api-key 등)은 환경변수로 오버라이드한다.
# 예: API_HTTPBIN_API_KEY=real-key

api:
  httpbin:
    base-url: https://httpbin.org
    connect-timeout: 3000   # 연결 타임아웃 (밀리초)
    read-timeout: 5000      # 읽기 타임아웃 (밀리초)
